<div class="flex flex-col min-h-screen bg-gray-50">
  <!-- Top Bar -->
  <header class="bg-amber-600 text-white p-4 shadow-md flex items-center justify-between">
    <h1 class="text-2xl font-bold">Restaurant Admin Dashboard</h1>
    <nav>
      <ul class="flex space-x-4">
        <li><button (click)="switchTab('items')" [class.font-semibold]="currentTab === 'items'" class="hover:underline">Manage Items</button></li>
        <li><button (click)="switchTab('cashiers')" [class.font-semibold]="currentTab === 'cashiers'" class="hover:underline">Manage Cashiers</button></li>
      </ul>
    </nav>
  </header>

  <main class="flex-1 p-6 max-w-6xl mx-auto w-full">
    <div *ngIf="currentTab === 'items'" class="flex flex-col md:flex-row md:space-x-8">
      <!-- Items List -->
      <section class="flex-1 bg-white rounded-lg shadow p-6 mb-6 md:mb-0">
        <h2 class="text-xl font-semibold mb-4 text-gray-700">Items List</h2>
        <table class="w-full border border-gray-300 rounded-md">
          <thead class="bg-amber-100 text-amber-800">
            <tr>
              <th class="p-3 border border-gray-300 text-left">Name</th>
              <th class="p-3 border border-gray-300 text-left">Price (₹)</th>
              <th class="p-3 border border-gray-300 text-left">Category</th>
              <th class="p-3 border border-gray-300 text-center">Actions</th>
            </tr>
          </thead>
          <tbody>
            <tr *ngFor="let item of items" class="hover:bg-gray-50">
              <td class="p-3 border border-gray-300">{{ item.name }}</td>
              <td class="p-3 border border-gray-300">{{ item.price }}</td>
              <td class="p-3 border border-gray-300">{{ item.category }}</td>
              <td class="p-3 border border-gray-300 text-center space-x-2">
                <button (click)="editItem(item)" class="px-2 py-1 bg-amber-600 text-white rounded hover:bg-amber-700">Edit</button>
                <button (click)="deleteItem(item.id)" class="px-2 py-1 bg-red-600 text-white rounded hover:bg-red-700">Delete</button>
              </td>
            </tr>
          </tbody>
        </table>
      </section>

      <!-- Add/Edit Item Form -->
      <section class="w-full md:w-1/3 bg-white rounded-lg shadow p-6">
        <h3 class="text-lg font-semibold mb-4 text-gray-700">{{ editingItemId === null ? 'Add New Item' : 'Edit Item' }}</h3>
        <form (ngSubmit)="addItem()" class="space-y-4">
          <div>
            <label class="block mb-1 font-medium text-gray-700">Name</label>
            <input [(ngModel)]="newItem.name" name="name" required class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-amber-500" />
          </div>
          <div>
            <label class="block mb-1 font-medium text-gray-700">Price (₹)</label>
            <input type="number" [(ngModel)]="newItem.price" name="price" required min="0" class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-amber-500" />
          </div>
          <div>
            <label class="block mb-1 font-medium text-gray-700">Category</label>
            <input [(ngModel)]="newItem.category" name="category" required class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-amber-500" />
          </div>
          <div class="flex space-x-2">
            <button type="submit" class="px-4 py-2 bg-amber-600 text-white rounded hover:bg-amber-700">
              {{ editingItemId === null ? 'Add Item' : 'Update Item' }}
            </button>
            <button type="button" (click)="cancelEdit()" class="px-4 py-2 bg-gray-300 rounded hover:bg-gray-400">
              Cancel
            </button>
          </div>
        </form>
      </section>
    </div>

    <div *ngIf="currentTab === 'cashiers'" class="bg-white rounded-lg shadow p-6 max-w-md mx-auto">
      <h2 class="text-xl font-semibold mb-4 text-gray-700">Cashiers List</h2>
      <table class="w-full border border-gray-300 rounded-md mb-6">
        <thead class="bg-amber-100 text-amber-800">
          <tr>
            <th class="p-3 border border-gray-300 text-left">Name</th>
            <th class="p-3 border border-gray-300 text-center">Actions</th>
          </tr>
        </thead>
        <tbody>
          <tr *ngFor="let cashier of cashiers" class="hover:bg-gray-50">
            <td class="p-3 border border-gray-300">{{ cashier.name }}</td>
            <td class="p-3 border border-gray-300 text-center space-x-2">
              <button (click)="editCashier(cashier)" class="px-2 py-1 bg-amber-600 text-white rounded hover:bg-amber-700">Edit</button>
              <button (click)="deleteCashier(cashier.id)" class="px-2 py-1 bg-red-600 text-white rounded hover:bg-red-700">Delete</button>
            </td>
          </tr>
        </tbody>
      </table>

      <h3 class="text-lg font-semibold mb-4 text-gray-700">{{ editingCashierId === null ? 'Add New Cashier' : 'Edit Cashier' }}</h3>
      <form (ngSubmit)="addCashier()" class="space-y-4 max-w-md">
        <div>
          <label class="block mb-1 font-medium text-gray-700">Name</label>
          <input [(ngModel)]="newCashier.name" name="cashierName" required class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-amber-500" />
        </div>
        <div class="flex space-x-2">
          <button type="submit" class="px-4 py-2 bg-amber-600 text-white rounded hover:bg-amber-700">
            {{ editingCashierId === null ? 'Add Cashier' : 'Update Cashier' }}
          </button>
          <button type="button" (click)="cancelEdit()" class="px-4 py-2 bg-gray-300 rounded hover:bg-gray-400">
            Cancel
          </button>
        </div>
      </form>
    </div>
  </main>
</div>
